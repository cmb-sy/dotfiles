# Cursor（VSCode と同一運用）

- **拡張機能**: `.vscode/extensions.zsh` を参照。`setup/install.zsh` の「Install Cursor extensions?」で一括インストール。
- **設定**: `~/.cursor/User/settings.json` は dotfiles の `.vscode/settings.json` へのシンボリックリンク（setup.zsh で作成）。

拡張の追加・設定の変更は `.vscode/` を編集すれば、VSCode と Cursor の両方に反映されます。
